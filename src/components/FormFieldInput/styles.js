import styled from 'styled-components'
import { Select } from 'atti-components'

export const StyledSelect = styled(Select)`
  background-color:white;

  &.transparent{
    background-color:transparent;
  }
`
